arr <- c(11, 12, 13, 14, 15)
arr + 2   # 每個數字都加 2
arr - 2   # 每個數字都減 2
arr * 2   # 每個數字都乘 2
arr / 2   # 每個數字都除以 2
arr**3    # 每個數字都立方
arr %% 2  # 每個數字除以 2 的餘數
arr %/% 2 # 每個數字除以 2 的商數
arr + arr # 對應位置的每個數字相加
arr * arr # 對應位置的每個數字相乘